# Server Configuration
NODE_ENV=development
PORT=3000
CLIENT_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=luharide
DB_USER=postgres
DB_PASSWORD=rahul@123

# Redis Configuration (Set to false to disable Redis)
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=luharide_dev_secret_key_change_in_production_2024
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=luharide_refresh_secret_key_change_in_production_2024
JWT_REFRESH_EXPIRES_IN=30d

# Admin: is email wale user ko Admin Dashboard milega (yahi email se login/signup = union_admin)
ADMIN_EMAIL=orahulpanwar@gmail.com

# Email OTP (Gmail SMTP) - send OTP to email for login/signup
EMAIL_USER=luharide@gmail.com
EMAIL_APP_PASSWORD=ghlrmxpyltpfsaun

# SMS Service (Twilio) - Add when ready
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Google Maps API - Add when ready
GOOGLE_MAPS_API_KEY=

# Razorpay Payment Gateway - Add when ready
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# AWS S3 Configuration - Add when ready
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
AWS_S3_BUCKET=luharide-documents

# Firebase Cloud Messaging - Add when ready
FCM_SERVER_KEY=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
