# Deploy to Railway

1. In Railway dashboard: open your **service** (e.g. luharide-app).
2. Go to **Settings** → **Source** (or **Build**).
3. Set **Root Directory** to: `backend`
4. Save. Redeploy.

After that, every push to GitHub will auto-deploy the backend.
